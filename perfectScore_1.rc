int myX; 
typedef int INT;
typedef float FLOAT;
function: void foo()
{
  const int x = 4;
  const float y = 1.5;
  //int y = 4;
  
  int a = 4;
  float b = 5;
  bool c;
  bool d;
  
  while (- (4 + 1.5)){}
  while ( -(a + b) ) {} // bool or int condition required
  while ( -(i + f) ) {} // bool or int condition required
 
  c = -(a + b);   // Error not assignable types Float to Bool
}
